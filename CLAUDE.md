# CLAUDE.md — 旅暦珈琲 TABIGOYOMI COFFEE

このファイルはClaude Code向けのプロジェクト指示書です。作業を始める前に必ず読んでください。

## このプロジェクトについて

Webデザイン制作会社(スタジオ)のポートフォリオ用に作成している、**架空企業の制作事例サイト**です。
実際に納品する案件ではなく、営業・実績提示用のサンプルサイトという位置づけです。実在の企業ではないため、
コピーや数値は自由に作り込んでよい前提です。

- 企業名:旅暦珈琲(たびごよみコーヒー) / TABIGOYOMI COFFEE
- 業態:横浜発のスペシャルティコーヒー焙煎所+スタンド
- コンセプト:毎月ひとつの産地だけに絞って豆を仕入れる「旅する暦」。焙煎士が実際に生産地を訪ね、
  その土地の物語を一杯に載せて届ける。在庫を増やさず、物語を削らない。
- サイトの目的:ブランディング重視のコーポレートサイト(集客LPではない)
- キャッチコピー(確定):「毎月、違う国の朝が届く。」

## デザインの方向性(最重要)

依頼者からの明確な指示:

1. **マイルド・ナチュラル系は禁止。** かっこよく、攻めたトーンにする。
2. **POPEYE(雑誌)のようなテイスト**を参照する。太めの見出し書体、エディトリアルなレイアウト、
   キャプション的なラベルの使い方など、雑誌の誌面デザインの語彙を借りる。
3. **映画のような背景**を演出する。ただし実写・動画素材は使わず、CSSのみで表現している
   (フィルムグレイン、レターボックス、ダイトーン風グラデーション、ケンバーンズ風のゆっくりしたズーム)。
4. **おもちゃっぽい表現はNG。** 太いクロスヘアや過剰な装飾は避け、細い線・上品なタイポグラフィで
   洗練させる方向に寄せること。地図パネルは等高線風の同心楕円+控えめなマーカーで表現済み。
5. 過去に「REC ●」の点滅表示とタイムコード風表記をヒーローに入れていたが、**不要と判断され削除済み**。
   同種の「撮影中っぽい」演出を新たに足す場合は必ず確認を取ること。
6. よくあるAI生成デザインの定番(生成りクリーム背景+テラコッタアクセントの組み合わせ、
   黒背景+単一ビビッドカラーだけの安易な配色)には安易に寄せない。今のダイトーン(黒〜レッド〜イエロー)の
   方向性を保つこと。

## デザイントークン

`app/globals.css` の `:root` に定義済み。変更する場合はここを起点に。

| 変数 | 値 | 用途 |
|---|---|---|
| `--bg` | `#120d0a` | 基本背景(シネマブラック) |
| `--paper` | `#f1ece0` | 明部・本文の反転色 |
| `--ink` | `#14100d` | ライトセクションの文字色 |
| `--red` | `#ef4123` | メインアクセント |
| `--yellow` | `#f2b705` | サブアクセント・ラベル |
| `--teal` | `#3c6e64` | ダイトーンのアクセント差し色 |

フォント:

- 見出し:`Anton`(極太コンデンス、雑誌の表紙感)
- キャプション・引用:`Fraunces`(イタリック、エディトリアルな柔らかさ)
- 本文:`Inter`
- ラベル・数値・搭乗券風要素:`Space Mono`

いずれもGoogle Fontsから`app/layout.js`で読み込み。フォントを追加・変更する場合もこの一箇所を編集すればよい。

## 現在の実装状況

Next.js 14(App Router)+ 素のCSS(Tailwind等は未導入)で構成済み。`npm run build` 通過確認済み。

```
app/
  layout.js    メタ情報・フォント読み込み
  page.js      1ページ構成のトップページ本体("use client"、ナビ開閉のみuseState)
  globals.css  デザイントークン含む全スタイル
```

セクション構成(`page.js` 内、上から順):

1. ヘッダー(スティッキーナビ、モバイルはハンバーガー開閉)
2. ヒーロー(キャッチコピー、レターボックス、グレイン、ケンバーンズ風背景アニメーション)
3. フィルムストリップ帯(産地の変遷を横並びで見せる)
4. コンセプトセクション(パラグラフ+搭乗券風カード)
5. 産地暦セクション(直近3ヶ月分の産地カード、ダイトーン背景)
6. 店頭メニュー(雑誌のギアガイド風リスト)
7. 店舗情報(住所等+等高線風の地図パネル)
8. フッター

## コーディング規約

- コンポーネント分割は現状していない(1ファイルの`page.js`)。今後セクションが増える場合は
  `app/components/` にセクション単位で切り出すのが妥当。
- CSSはグローバル1枚(`globals.css`)。命名は機能ベースの素朴なクラス名(`.stamp-card`, `.hero-lede` 等)。
  CSS Modulesへの移行は必須ではないが、コンポーネント分割時に検討してよい。
- アクセシビリティ:`:focus-visible` のアウトライン設定済み、`prefers-reduced-motion` で
  アニメーションを止める分岐を入れてある。新しいアニメーションを足す場合も同様に対応すること。
- レスポンシブ:760px(ナビ)、900px/860px(グリッド系)、600px を基準にブレークポイントを設定済み。

## 今後のタスク候補

依頼者と要相談。以下は選択肢の例(未確定):

- 実写・自作イラスト等の画像素材を追加する場合、ダイトーン背景の一部を差し替える形で対応
- 他業界(美容・士業など)の架空企業サイトを同シリーズとして追加制作
- 複数ページ化(店舗一覧、産地暦のアーカイブページなど)する場合はApp Routerのルーティングを追加

**Claude Codeで作業を始める際は、上記のうちどのタスクに着手するかを最初に確認すること。**
指示が無い場合、既存デザインのトーン・トークンを崩す変更は行わない。

## コマンド

```bash
npm install
npm run dev     # http://localhost:3000
npm run build
```

## デプロイ

Vercelへそのままインポートで動作する想定(特別な設定は不要)。
